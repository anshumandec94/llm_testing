# Metric dictionary

This file explains every metric and artifact column produced by `sim.runner.SimulationRunner` and `sim.hpo.run_hpo`.

Three run types produce metrics:

- **Full simulation** (`experiment_profile=full`): the closed-loop experiment with attendance, actions, feedback, and retraining. Metrics are logged **per round** as MLflow time-series steps.
- **`recommender_only`** (`experiment_profile=recommender_only`): a first-batch diagnostic pass over held-out items with no simulated feedback loop.
- **HPO** (run via `sim.hpo.run_hpo`): wraps many `recommender_only` candidate runs and logs summary metrics for the winning config.

---

## Shared metric

| Metric | Profile | Meaning | Example |
| --- | --- | --- | --- |
| `elapsed_seconds` | both | Wall-clock run time for the full experiment run. | `180.4` means the run took about 3 minutes. |

---

## Full simulation metrics

Logged per round (MLflow step = round number).

### Attendance and engagement

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `attendance_rate` | Fraction of eval users who attended in that round. | Higher means more users showed up and were eligible to receive recommendations. | `0.72` means 72% of eval users attended. |
| `mean_attention_consumed` | Mean attention budget spent by attending users during the round. | Higher means users spent more of their limited attention budget. | `0.9` means users nearly exhausted one unit of attention on average. |

### Retrieval quality (attending users, first batch only)

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `hit_rate` | Mean per-user fraction of held-out items recovered by the **first** recommendation batch. | Higher is better. `0` means the first batch never surfaced held-out items. | `0.10` means a user recovered 10% of their held-out set on average. |
| `ndcg_at_{k}` | Mean ranking quality of the first batch, discounted by rank. `k = rec_list_size`. | Higher is better; hits near the top count more. | `ndcg_at_6 = 0.35` is much better than `0.05`. |
| `holdout_recall` | Mean fraction of each user's held-out set surfaced **across the whole round**, including re-requests. | Higher means the round exposed more relevant items somewhere in the round. | `0.25` means one quarter of held-out items were surfaced by the end of the round. |

### Feedback

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `mean_signal_strength` | Mean numeric feedback value generated by explicit `rate` actions in that round. | Higher means stronger positive ratings were sent back to the recommender. | `3.8` means ratings averaged 3.8 out of 5. |
| `action_watch_frac` | Fraction of all actions that were `watch`. | The action mix helps explain feedback quality and user behavior. | `0.50` means half of all actions were watches. |
| `action_rate_frac` | Fraction of all actions that were `rate`. | Higher means more explicit ratings were generated. | `0.20` means 20% of actions were ratings. |
| `action_addlist_frac` | Fraction of all actions that were `add_to_list`. | Higher means more medium-strength positive intent without immediate watching. | `0.30` means 30% of actions were add-to-list events. |

---

## `recommender_only` metrics

### Retrieval and ranking quality

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `user_count` | Number of eval users included in the diagnostic run. | Mostly a coverage / sanity-check metric. | `12834` means diagnostics were computed for 12,834 users. |
| `hit_rate` | Mean per-user fraction of held-out items hit by the first recommendation batch. | Higher is better. Same interpretation as in the full simulation profile. | `0.02` means 2% of held-out items were recovered on average. |
| `ndcg_at_{k}` | Mean first-batch ranking quality against held-out items. | Higher is better. | `0.11` beats `0.03`. |
| `fraction_users_with_holdout_hit` | Fraction of users who got **at least one** held-out hit in the first batch. | Higher is better and easier to interpret than raw hit rate. | `0.18` means 18% of users saw at least one held-out item. |

### Popularity diagnostics

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `mean_user_recommended_popularity_mean` | Mean of each user's average recommendation popularity (training-set interaction count). | Higher means the recommender tends to surface more popular items. | `4000` means the average recommended item had about 4,000 training interactions. |
| `mean_user_recommended_popularity_std` | Mean within-user spread of recommendation popularity. | Higher means the first batch mixes blockbuster and niche items more broadly. | `2500` means popularity within a user's batch varies a lot. |
| `mean_user_heldout_popularity_mean` | Mean of each user's average held-out-item popularity. | This is the "what the user actually consumed later" popularity baseline. | `16000` means held-out items were much more popular than the recommendations. |
| `mean_user_heldout_popularity_std` | Mean within-user spread of held-out-item popularity. | Helps show whether held-out behavior itself is narrow or broad in popularity space. | `5000` means users' held-out items vary widely in popularity. |
| `mean_user_comparison_popularity_mean` | Legacy alias of `mean_user_heldout_popularity_mean`. | Kept for backward compatibility with earlier reports. | Same value as the held-out popularity mean. |
| `mean_user_comparison_popularity_std` | Legacy alias of `mean_user_heldout_popularity_std`. | Kept for backward compatibility with earlier reports. | Same value as the held-out popularity std. |
| `mean_user_popularity_mean_delta` | Mean of `recommended_popularity_mean − heldout_popularity_mean` across users. | Negative means recommendations are less popular than users' held-out histories; positive means more popular. | `-12000` means recommendations were much less popular than the held-out baseline. |
| `std_user_popularity_mean_delta` | Cross-user spread of popularity deltas. | Higher means popularity mismatch varies a lot across users. | `8000` means some users get much more niche or much more mainstream lists than others. |
| `fraction_users_recommended_mean_gt_comparison_mean` | Fraction of users whose average recommended popularity exceeded their held-out average popularity. | Above `0.5` means the recommender is more popularity-seeking than users' held-out behavior for most users. | `0.11` means only 11% of users received more popular recommendations than their held-out baseline. |

### Residual-target diagnostics

For these metrics, the held-out target is **debiased first**:

`debiased_rating = observed_rating − (global_bias + user_bias + item_bias)`

This makes the target comparable to latent dot-product scores, which live in residual / affinity space rather than raw 1–5 rating space.

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `mean_user_heldout_debiased_rating_mean` | Mean of each user's average debiased held-out rating. | Near `0` means the held-out set is close to the train-set bias baseline on average. | `0.12` means held-out ratings were slightly above baseline. |
| `mean_user_heldout_debiased_rating_std` | Mean within-user spread of debiased held-out ratings. | Higher means held-out residual preference strength varies more within each user. | `0.48` means held-out residuals span a moderate range. |
| `mean_user_heldout_recommender_score_mean` | Mean of the recommender latent residual scores on each user's held-out items. | Higher means the recommender assigns stronger residual affinity to held-out items. | `0.08` means held-out items look mildly attractive in recommender latent space. |
| `mean_user_heldout_recommender_score_std` | Mean within-user spread of recommender residual scores over held-out items. | Higher means the recommender distinguishes strongly between a user's held-out items. | `0.30` means some held-out items score much higher than others. |
| `mean_user_heldout_internal_score_mean` | Mean of the internal SVD latent scores on each user's held-out items. | Higher means the internal preference model scores held-out items more strongly. | `0.42` means the internal model sees strong affinity in its own latent space. |
| `mean_user_heldout_internal_score_std` | Mean within-user spread of internal latent scores over held-out items. | Higher means the internal model sharply separates stronger and weaker held-out items. | `0.15` means internal scores vary, but less than the recommender example above. |
| `mean_user_heldout_score_mean_gap` | Mean of `heldout_recommender_score_mean − heldout_internal_score_mean` across users. | Negative means the internal model tends to score held-out items higher than the recommender; positive means the reverse. | `-0.35` means the internal model is more favorable to held-out items on average. |
| `std_user_heldout_score_mean_gap` | Cross-user spread of score-mean gaps. | Higher means the relative advantage of one model over the other varies across users. | `0.22` means some users favor the recommender while others strongly favor the internal model. |
| `fraction_users_recommender_score_mean_gt_internal` | Fraction of users for whom the recommender's mean held-out score exceeded the internal model's. | Above `0.5` means the recommender scores held-out items higher in its own latent space for most users. | `0.44` means the internal model wins for most users. |

### Residual correlation diagnostics

| Metric | Meaning | How to read it | Example |
| --- | --- | --- | --- |
| `mean_user_heldout_recommender_residual_pearson` | Mean per-user Pearson correlation between debiased held-out ratings and recommender residual scores. | Higher is better if you care about linear agreement in residual space. | `0.18` means modest linear alignment. |
| `mean_user_heldout_recommender_residual_spearman` | Mean per-user Spearman rank correlation between debiased held-out ratings and recommender residual scores. | Higher is better if you care about relative ordering more than exact spacing. | `0.25` means the recommender roughly preserves ranking order in residual space. |
| `mean_user_heldout_internal_residual_pearson` | Mean per-user Pearson correlation between debiased held-out ratings and internal latent scores. | Lets you compare the internal model's residual alignment to the recommender's. | `0.31` means the internal model tracks residual preference better than the previous recommender example. |
| `mean_user_heldout_internal_residual_spearman` | Mean per-user Spearman correlation between debiased held-out ratings and internal latent scores. | Higher means stronger monotonic agreement. | `0.34` means the internal model ranks stronger residual items more consistently. |
| `global_heldout_recommender_residual_pearson` | One Pearson correlation computed across **all** held-out rows, ignoring user boundaries. | Useful as an overall signal, but can hide user-level heterogeneity. | `0.09` means weak global linear alignment. |
| `global_heldout_recommender_residual_spearman` | One Spearman correlation across all held-out rows for the recommender. | Good for coarse global rank agreement. | `0.14` means weak but positive ordering alignment. |
| `global_heldout_internal_residual_pearson` | One Pearson correlation across all held-out rows for the internal model. | Direct global comparator to the recommender value above. | `0.22` means stronger global linear alignment than the recommender example above. |
| `global_heldout_internal_residual_spearman` | One Spearman correlation across all held-out rows for the internal model. | Direct global comparator to the recommender rank alignment. | `0.28` means stronger global rank alignment than the recommender example above. |
| `fraction_users_recommender_residual_pearson_gt_internal` | Fraction of users for whom the recommender's per-user Pearson residual correlation beat the internal model's. | Above `0.5` means the recommender wins this comparison for most users. | `0.27` means the internal model wins for most users. |

---

## HPO metrics

These are logged on the parent `hpo-search` MLflow run. Each candidate also logs the full `recommender_only` metric set above on its own nested run.

| Metric | Meaning | How to read it |
| --- | --- | --- |
| `best_validation_{metric_name}` | Value of the selection metric (e.g. `best_validation_ndcg_at_6`) for the winning candidate on the **validation** split. | Higher is better; this is the score that drove the selection. |
| `best_validation_fraction_users_with_holdout_hit` | Fraction of users with at least one hit for the winning candidate on the validation split. | Higher is better. Useful sanity-check alongside the primary metric. |
| `best_final_{metric_name}` | Value of the selection metric (e.g. `best_final_ndcg_at_6`) for the winning config re-run on the **held-out** split. | The number you should compare across HPO runs. |
| `best_final_fraction_users_with_holdout_hit` | Fraction of users with at least one held-out hit for the winning config on the final split. | Companion coverage metric for the final evaluation. |

---

## Artifact files

### `recommender_only` artifacts

#### `diagnostics/recommender_only_user_diagnostics.parquet`

One row per simulated user. All per-user metrics before aggregation.

| Column | Meaning |
| --- | --- |
| `evaluation_split` | Which split was used (`validation` or `held_out`). |
| `userId` | Base user ID from the dataset. |
| `simulation_user_id` | Simulated user ID (equals `userId` in `one_to_one` mode; negative int in `one_per_agent_type` mode). |
| `agent_type` | Agent type assigned to this simulation user. |
| `recommended_item_count` | Number of items in the first recommendation batch. |
| `heldout_item_count` | Number of held-out items for this user. |
| `hit_rate` | Per-user first-batch hit rate. |
| `ndcg_at_{k}` | Per-user NDCG at `rec_list_size`. |
| `recommended_popularity_mean` | Mean training-set popularity of recommended items. |
| `recommended_popularity_std` | Std of training-set popularity of recommended items. |
| `heldout_popularity_mean` | Mean training-set popularity of held-out items. |
| `heldout_popularity_std` | Std of training-set popularity of held-out items. |
| `popularity_mean_delta` | `recommended_popularity_mean − heldout_popularity_mean`. |
| `heldout_debiased_rating_mean` | Mean debiased held-out rating. |
| `heldout_debiased_rating_std` | Std of debiased held-out ratings. |
| `heldout_recommender_scored_count` | Number of held-out items that had a valid recommender score (non-NaN). |
| `heldout_internal_scored_count` | Number of held-out items that had a valid internal model score (non-NaN). |
| `heldout_recommender_score_mean` | Mean recommender residual score over held-out items. |
| `heldout_recommender_score_std` | Std of recommender residual scores over held-out items. |
| `heldout_internal_score_mean` | Mean internal SVD score over held-out items. |
| `heldout_internal_score_std` | Std of internal SVD scores over held-out items. |
| `heldout_recommender_residual_pearson` | Per-user Pearson corr between debiased ratings and recommender scores. |
| `heldout_recommender_residual_spearman` | Per-user Spearman corr between debiased ratings and recommender scores. |
| `heldout_internal_residual_pearson` | Per-user Pearson corr between debiased ratings and internal scores. |
| `heldout_internal_residual_spearman` | Per-user Spearman corr between debiased ratings and internal scores. |
| `heldout_score_mean_gap` | `heldout_recommender_score_mean − heldout_internal_score_mean`. |
| `comparison_item_count` | Backward-compat alias for `heldout_item_count`. |
| `comparison_popularity_mean` | Backward-compat alias for `heldout_popularity_mean`. |
| `comparison_popularity_std` | Backward-compat alias for `heldout_popularity_std`. |

#### `recs/recommender_only_recommendations.parquet`

One row per recommended item per user.

| Column | Meaning |
| --- | --- |
| `evaluation_split` | Which split was used (`validation` or `held_out`). |
| `userId` | Base user ID. |
| `simulation_user_id` | Simulated user ID. |
| `agent_type` | Agent type for this user. |
| `rank` | Position of this item in the recommendation list (1 = top). |
| `movieId` | Recommended item ID. |
| `popularity` | Training-set interaction count for this item. |
| `is_held_out_hit` | `1` if this item is in the user's held-out set, `0` otherwise. |

#### `diagnostics/recommender_only_heldout_scores.parquet`

One row per held-out item per user. Used for the residual-correlation metrics.

| Column | Meaning |
| --- | --- |
| `evaluation_split` | Which split was used (`validation` or `held_out`). |
| `userId` | Base user ID. |
| `simulation_user_id` | Simulated user ID. |
| `agent_type` | Agent type for this user. |
| `movieId` | Held-out item ID. |
| `rating` | Raw held-out rating (1–5 scale). |
| `rating_bias` | Train-set bias for this user/item pair: `global_bias + user_bias + item_bias`. |
| `debiased_rating` | `rating − rating_bias`. This is the residual target used for all correlation metrics. |
| `recommender_score` | Recommender latent residual score (`user_factor · item_factor`). `NaN` if the item has no factor. |
| `internal_score` | Internal SVD latent affinity score. `NaN` if the item has no factor. |

---

### Full simulation artifacts

#### `recs/recs_round_{NNN}.parquet`

One row per candidate item per request per attending user. Written every round.

| Column | Meaning |
| --- | --- |
| `round` | Round number (1-indexed). |
| `request` | Request number within the round (1 = first batch, 2 = re-request, etc.). |
| `userId` | Base user ID. |
| `simulation_user_id` | Simulated user ID. |
| `agent_type` | Agent type for this user. |
| `movieId` | Candidate item ID. |
| `rank` | Position in the agent-ranked list for this request (1 = highest-scored). |
| `is_held_out` | `True` if this item is in the user's held-out set. |
| `interaction` | Action taken (`watch`, `rate`, `add_to_list`) or `None` if ignored. |
| `signal` | Numeric signal for the action, or `None` if ignored. Explicit ratings are on a 1–5 scale; `watch` and `add_to_list` use configured constants but do **not** feed back into model updates. |

#### `recs/recs_round_{NNN}_subgroups.parquet`

Aggregated per-agent-type summary for each round. Useful for comparing agent types within a mixed-population run.

| Column | Meaning |
| --- | --- |
| `agent_type` | Agent type label. |
| `shown_count` | Total number of items shown to users of this agent type in the round. |
| `heldout_hit_count` | Total number of those items that were in a user's held-out set. |
| `round` | Round number. |

---

### Shared artifacts

#### `summary.csv`

One row per round (full simulation) or one row total (recommender_only). Contains all MLflow metrics for the run in a flat CSV — useful for quick analysis without the MLflow UI.
